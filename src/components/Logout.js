import React from 'react';

const Logout = () => {
  console.log('oi logout');

  return (
    <div>
      <h1>Tri Tracker</h1>
      <button type="button">Logout</button>
    </div>
  );
};

export default Logout;
